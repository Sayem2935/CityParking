# Mock Parking Data Audit Report

**Date:** 2026-06-14  
**Scope:** Full-stack (React frontend + Spring Boot backend)

---

## 1. Complete Inventory of Hardcoded Parking-Related Values

### 1.1 Frontend: `src/types/parking.types.ts` — Lines 81-96

**Source Type:** Hardcoded constant (`INITIAL_MOCK_STATS`)

| Mock Value | Line | Code |
|---|---|---|
| Total Slots | 83 | `totalSlots: 200` |
| Available Slots | 84 | `availableSlots: 142` |
| Occupied Slots | 85 | `occupiedSlots: 58` |
| Utilization | 86 | `utilization: 29` |
| Today Entries | 87 | `todayEntries: 37` |
| Today Exits | 88 | `todayExits: 22` |
| Zone A utilization | 90 | `zoneA: { utilization: 35 }` |
| Zone B utilization | 91 | `zoneB: { utilization: 45 }` |
| Zone C utilization | 92 | `zoneC: { utilization: 25 }` |
| Zone D utilization | 93 | `zoneD: { utilization: 20 }` |
| VIP utilization | 94 | `vip: { utilization: 10 }` |
| Handicap utilization | 95 | `handicap: { utilization: 5 }` |

**Exact Code:**
```typescript
// Line 81-96
export const INITIAL_MOCK_STATS: ParkingStatistics = {
  totalSlots: 200,
  availableSlots: 142,
  occupiedSlots: 58,
  utilization: 29,
  todayEntries: 37,
  todayExits: 22,
  zoneUtilization: {
    zoneA: { utilization: 35 },
    zoneB: { utilization: 45 },
    zoneC: { utilization: 25 },
    zoneD: { utilization: 20 },
    vip: { utilization: 10 },
    handicap: { utilization: 5 },
  },
};
```

---

### 1.2 Frontend: `src/services/parking.service.ts` — Lines 112-130

**Source Type:** Mock fallback in service layer (returned when API fails)

| Mock Value | Line | Code |
|---|---|---|
| Zone A total | 115 | `total: 50` |
| Zone A free | 115 | `free: 35` |
| Zone A occupied | 115 | `occupied: 15` |
| Zone B total | 116 | `total: 40` |
| Zone B free | 116 | `free: 28` |
| Zone B occupied | 116 | `occupied: 12` |

**Exact Code:**
```typescript
// Lines 112-130
private mockFallback(): ParkingAvailability {
  return {
    totalSlots: 90,
    occupiedSlots: 27,
    freeSlots: 63,
    zoneBreakdown: {
      'Zone A': { total: 50, free: 35, occupied: 15 },
      'Zone B': { total: 40, free: 28, occupied: 12 },
    },
    lastUpdated: new Date().toISOString(),
  };
}
```

**Note:** This `mockFallback()` is called on **every** API error in all three methods: `getAvailability()` (line 71), `getAllSlots()` (line 84), and `getStatistics()` (line 101).

---

### 1.3 Frontend: `src/pages/LandingPage.tsx` — Lines 14-19

**Source Type:** Hardcoded in React component (marketing/hero stats)

| Mock Value | Line | Code |
|---|---|---|
| AI Models Active | 16 | `value: '4'` |
| Detection Accuracy | 17 | `value: '98.2', suffix: '%'` |
| Vehicle Types | 18 | `value: '2'` |
| Response Time | 19 | `value: '<200', suffix: 'ms'` |

**Exact Code:**
```typescript
// Lines 14-19
const stats = [
  { label: 'AI Models Active', value: '4', suffix: '' },
  { label: 'Detection Accuracy', value: '98.2', suffix: '%' },
  { label: 'Vehicle Types', value: '2', suffix: '' },
  { label: 'Response Time', value: '<200', suffix: 'ms' },
];
```

---

### 1.4 Backend: `MockGeminiService.java` — Lines 79-117

**Source Type:** Generated by mock service (activated when `ai.provider.vision=mock`)

| Mock Value | Line | Code |
|---|---|---|
| Total Slots | 82 | `int totalSlots = 20;` |
| Occupied Slots | 83 | `int occupied = 8 + ThreadLocalRandom.current().nextInt(8);` (8-15) |
| Free Slots | 84 | `int free = totalSlots - occupied;` |
| Confidence | 113 | `result.setConfidence(0.93);` |
| 20 individual slot details | 86-107 | Slots A1-A4, B1-B4, C1-C4, D1-D4, VIP1-VIP2, H1-H2 |

**Exact Code (key sections):**
```java
// Lines 79-117
@Override
public ParkingDetectionResult detectParkingSlots(MultipartFile image) {
    int totalSlots = 20;
    int occupied = 8 + ThreadLocalRandom.current().nextInt(8); // 8-15 occupied
    int free = totalSlots - occupied;

    List<ParkingDetectionResult.SlotDetail> slots = Arrays.asList(
            new ParkingDetectionResult.SlotDetail("A1", false, 0.97, "Zone-A"),
            new ParkingDetectionResult.SlotDetail("A2", true, 0.95, "Zone-A"),
            // ... 18 more hardcoded slots ...
            new ParkingDetectionResult.SlotDetail("H2", true, 0.96, "Handicap")
    );

    ParkingDetectionResult result = new ParkingDetectionResult();
    result.setTotalSlots(totalSlots);
    result.setOccupiedSlots(occupied);
    result.setFreeSlots(free);
    result.setConfidence(0.93);
    result.setSlots(slots);
    return result;
}
```

---

### 1.5 Backend: `ParkingSlotService.java` — Lines 122-155, 176-189

**Source Type:** Returned by mock backend service (fallback when DB has no slots)

| Mock Value | Line | Code |
|---|---|---|
| Zone A total/free | 124-125 | `total: 50, free: 35` |
| Zone B total/free | 126-127 | `total: 40, free: 28` |
| Zone C total/free | 128-129 | `total: 30, free: 22` |
| Zone D total/free | 130-131 | `total: 20, free: 15` |
| VIP total/free | 132 | `total: 10, free: 8` |
| Mock Statistics | 144-155 | totalSlots: 150, occupiedSlots: 55, availableSlots: 95, utilization: 37 |
| Mock Occupancy | 176-189 | totalSlots: 150, occupiedSlots: 50, availableSlots: 100, zones with hardcoded data |

**Exact Code:**
```java
// Lines 118-133 — getZoneBreakdown() mock fallback
private Map<String, AvailabilityResponse.ZoneInfo> getMockZoneBreakdown() {
    Map<String, AvailabilityResponse.ZoneInfo> zones = new LinkedHashMap<>();
    zones.put("Zone A", new AvailabilityResponse.ZoneInfo(50, 35, 15));
    zones.put("Zone B", new AvailabilityResponse.ZoneInfo(40, 28, 12));
    zones.put("Zone C", new AvailabilityResponse.ZoneInfo(30, 22, 8));
    zones.put("Zone D", new AvailabilityResponse.ZoneInfo(20, 15, 5));
    zones.put("VIP", new AvailabilityResponse.ZoneInfo(10, 8, 2));
    return zones;
}

// Lines 141-155 — getStatistics() mock fallback
private ParkingStatisticsResponse getMockStatistics() {
    ParkingStatisticsResponse stats = new ParkingStatisticsResponse();
    stats.setTotalSlots(150);
    stats.setOccupiedSlots(55);
    stats.setAvailableSlots(95);
    stats.setUtilization(37);
    // ... zone utilization map ...
}

// Lines 170-189 — getCurrentOccupancy() mock fallback
private AvailabilityResponse getMockOccupancy() {
    AvailabilityResponse mock = new AvailabilityResponse();
    mock.setTotalSlots(150);
    mock.setOccupiedSlots(50);
    mock.setAvailableSlots(100);
    mock.setLastUpdated(LocalDateTime.now().toString());
    // ... zone breakdown ...
}
```

---

### 1.6 Backend: Database Migration V12.5 — `V12.5__expand_parking_zone_columns.sql`

**Source Type:** Seeded into database (migration default value)

| Mock Value | Line | Code |
|---|---|---|
| Default zone name | 7 | `ALTER TABLE parking_slots ADD COLUMN zone_name VARCHAR(100) NOT NULL DEFAULT 'AB4 Parking Area';` |

**Exact Code:**
```sql
-- Line 7
ALTER TABLE parking_slots ADD COLUMN zone_name VARCHAR(100) NOT NULL DEFAULT 'AB4 Parking Area';
```

---

### 1.7 Backend: Database Migration V13 — `V13__university_parking_customization.sql`

**Source Type:** Seeded into database (UPDATE statements in migration)

| Mock Value | Line | Code |
|---|---|---|
| Zone A name | 20 | `'AB4 Parking Area'` |
| Zone B name | 21 | `'AB4 Parking Area'` |
| Zone C name | 22 | `'Engineering Parking Area'` |
| Floor 1 zone_name | 25 | `'AB4 Parking Area'` |
| Floor 1 floor_name | 26 | `'Floor 1'` |
| Floor 2 zone_name | 29 | `'AB4 Parking Area'` |
| Floor 2 floor_name | 30 | `'Floor 2'` |
| university_name | 36 | `'City University'` |
| university_short_name | 37 | `'CU'` |
| building_name | 40 | `'AB4 Building'` |
| parking_area_name | 41 | `'AB4 Parking Area'` |
| Slot code prefix | 44 | `'CU'` |

**Exact Code:**
```sql
-- Lines 19-22: Update zone names
UPDATE parking_slots SET zone_name = 'AB4 Parking Area' WHERE zone = 'A';
UPDATE parking_slots SET zone_name = 'AB4 Parking Area' WHERE zone = 'B';
UPDATE parking_slots SET zone_name = 'Engineering Parking Area' WHERE zone = 'C';

-- Lines 25-30: Update floor names
UPDATE parking_slots SET floor_name = 'Floor 1', zone_name = 'AB4 Parking Area' WHERE floor_number = 1;
UPDATE parking_slots SET floor_name = 'Floor 2', zone_name = 'AB4 Parking Area' WHERE floor_number = 2;
```

---

### 1.8 Backend: Database Migration V7 — `V7__create_parking_slot_tables.sql` — Lines 70-153

**Source Type:** Seeded into database (INSERT statements)

| Mock Value | Lines | Code |
|---|---|---|
| 75 parking slots total | 72-153 | 12+12+9+8+8+7+7+7+5 slots across 3 floors, zones A/B/C |
| Slot types | various | STANDARD, COMPACT, HANDICAPPED, EV_CHARGING, VIP |
| Zone A slots (Floor 1) | 72-84 | A01-A12 (12 slots) |
| Zone B slots (Floor 1) | 87-99 | B01-B12 (12 slots) |
| Zone C slots (Floor 1) | 102-111 | C01-C09 (9 slots) |
| Zone A slots (Floor 2) | 114-122 | A13-A20 (8 slots) |
| Zone B slots (Floor 2) | 125-133 | B13-B20 (8 slots) |
| Zone A slots (Floor 3) | 136-143 | A21-A27 (7 slots) |
| Zone B slots (Floor 3) | 146-153 | B21-B27 (7 slots) |

---

## 2. Data Classification Summary

| Category | File | Type |
|---|---|---|
| Hardcoded in React types | `src/types/parking.types.ts:81-96` | `INITIAL_MOCK_STATS` constant |
| Hardcoded in frontend service | `src/services/parking.service.ts:112-130` | `mockFallback()` method |
| Hardcoded in React component | `src/pages/LandingPage.tsx:14-19` | Marketing stats array |
| Generated by mock service | `backend/.../ai/MockGeminiService.java:79-117` | `detectParkingSlots()` |
| Returned by mock backend | `backend/.../service/ParkingSlotService.java:122-189` | 3 mock fallback methods |
| Seeded into database | `backend/.../migration/V7__*.sql:70-153` | 75 parking slot INSERTs |
| Seeded into database | `backend/.../migration/V12.5__*.sql:7` | Default zone_name column |
| Seeded into database | `backend/.../migration/V13__*.sql:19-44` | Zone names + university config |

---

## 3. Parking Endpoints Currently Returning Mock/Demo Data

| # | Endpoint | Method | Controller | Service Method | Mock Behavior |
|---|---|---|---|---|---|
| 1 | `/api/parking/availability` | GET | `ParkingController.java` | `ParkingSlotService.getAvailability()` | Falls back to `getMockOccupancy()` when DB has no slots |
| 2 | `/api/parking/zone-breakdown` | GET | `ParkingController.java` | `ParkingSlotService.getZoneBreakdown()` | Falls back to `getMockZoneBreakdown()` when DB has no slots |
| 3 | `/api/parking/statistics` | GET | `ParkingController.java` | `ParkingSlotService.getStatistics()` | Falls back to `getMockStatistics()` when DB has no slots |
| 4 | `/api/parking/slots` | GET | `ParkingController.java` | `ParkingSlotService.getAllSlots()` | Falls back to `getMockSlotList()` when DB has no slots |
| 5 | `/api/parking/scan` | POST | `ParkingController.java` | Uses `GeminiService.detectParkingSlots()` | Returns mock detection when `ai.provider.vision=mock` (default) |

**Frontend fallback:** If any of the above API calls fail (network error, 500, etc.), the frontend `parkingService.ts` returns its own `mockFallback()` data (lines 112-130).

---

## 4. Existing PostgreSQL Entities & Repositories (Already Available for Real Data)

| Entity | Repository | Table | Key Fields |
|---|---|---|---|
| `ParkingSlot.java` | `ParkingSlotRepository.java` | `parking_slots` | slotCode, slotType, status, floorNumber, zone, zoneName, floorName, coordinatesJson |
| `ParkingAssignment.java` | `ParkingAssignmentRepository.java` | `parking_assignments` | userId, vehicleId, slotId, assignedAt, releasedAt, status |
| `ParkingScanLog.java` | `ParkingScanLogRepository.java` | `parking_scan_log` | totalSlots, occupiedSlots, freeSlots, detectionsJson, scannedAt |
| `ParkingOccupancyHistory.java` | `ParkingOccupancyHistoryRepository.java` | `parking_occupancy_history` | zone, totalSlots, occupiedSlots, freeSlots, recordedAt |

---

## 5. Migration Plan: Mock Source → Real Database Source

### Phase 1: Remove Frontend Mock Fallbacks

| Current Mock Source | Real Source | Action |
|---|---|---|
| `parking.types.ts` INITIAL_MOCK_STATS (line 81) | API response from `/api/parking/statistics` | Remove constant; load from API only |
| `parking.service.ts` mockFallback() (line 112) | API response from `/api/parking/availability` | Remove method; let errors propagate to UI error state |
| `LandingPage.tsx` stats (line 14) | Static marketing values (keep as-is) | No change needed — these are not parking data |

**Files to modify:**
- `src/types/parking.types.ts` — Delete `INITIAL_MOCK_STATS` export
- `src/services/parking.service.ts` — Delete `mockFallback()` method; update catch blocks to throw instead of silently returning mock data

### Phase 2: Remove Backend Mock Service Fallbacks

| Current Mock Source | Real Source | Action |
|---|---|---|
| `ParkingSlotService.java` `getMockZoneBreakdown()` | `ParkingSlotRepository` COUNT queries by zone and status | Delete `getMockZoneBreakdown()`; always query DB |
| `ParkingSlotService.java` `getMockStatistics()` | `ParkingSlotRepository` + `ParkingAssignmentRepository` queries | Delete `getMockStatistics()`; always query DB |
| `ParkingSlotService.java` `getMockOccupancy()` | `ParkingSlotRepository` COUNT queries | Delete `getMockOccupancy()`; always query DB |
| `ParkingSlotService.java` `getMockSlotList()` | `ParkingSlotRepository.findAll()` | Delete `getMockSlotList()`; always query DB |

**Files to modify:**
- `backend/.../service/ParkingSlotService.java` — Remove all 4 `getMock*()` methods; remove `if (zones.isEmpty())` fallback checks

### Phase 3: Replace MockGeminiService Parking Detection

| Current Mock Source | Real Source | Action |
|---|---|---|
| `MockGeminiService.detectParkingSlots()` | `GeminiServiceImpl.detectParkingSlots()` (real Gemini API) or `ParkingSlotRepository` query | Set `ai.provider.vision=gemini` in production; keep mock only for dev/test profiles |

**Files to modify:**
- `backend/src/main/resources/application.yml` — Set `ai.provider.vision: gemini` for production profile
- No code changes needed to `MockGeminiService.java` — it's already gated by `@ConditionalOnProperty`

### Phase 4: Validate Seed Data vs. Real Data

| Current Seed Data | Action |
|---|---|
| V7 migration: 75 slots (A01-A27, B01-B27, C01-C09) | **Keep** — These are real slot definitions, not mock data |
| V12.5 migration: default `zone_name = 'AB4 Parking Area'` | **Keep** — Real column default |
| V13 migration: zone names, floor names, university config | **Keep** — Real configuration data |

**No changes needed** for migration files. The seeded slots ARE the real data source. The mock fallbacks exist only as safety nets when the DB is empty (which shouldn't happen after V7 runs).

### Phase 5: Ensure Real Queries Cover All Use Cases

Add these repository queries if not already present:

```java
// ParkingSlotRepository.java — add if missing:
@Query("SELECT s.zone, s.zoneName, COUNT(s), SUM(CASE WHEN s.status = 'FREE' THEN 1 ELSE 0 END) FROM ParkingSlot s GROUP BY s.zone, s.zoneName")
List<Object[]> countByZoneAndStatus();

@Query("SELECT COUNT(s) FROM ParkingSlot s WHERE s.status = :status")
long countByStatus(@Param("status") String status);
```

---

## 6. Summary: What to Keep vs. Remove

| Item | Keep/Remove | Reason |
|---|---|---|
| V7 migration seed slots | **KEEP** | Real slot definitions for the parking facility |
| V12.5/V13 zone names | **KEEP** | Real configuration (AB4 Parking Area, Engineering Parking Area) |
| `INITIAL_MOCK_STATS` constant | **REMOVE** | Unused after connecting to real API |
| `parking.service.ts` `mockFallback()` | **REMOVE** | Hides API failures from users |
| `ParkingSlotService` 4 mock methods | **REMOVE** | Redundant — DB is always seeded by V7 |
| `MockGeminiService` parking detection | **KEEP** (dev only) | Useful for testing without Gemini API key |
| `LandingPage.tsx` marketing stats | **KEEP** | Not parking operational data |